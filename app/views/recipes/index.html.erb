<% set_page_title "All Recipes" %>

<ul>
  <% recipes.each do |recipe| %>
    <li>
      <%= link_to recipe_path(recipe) do %>
        <h2><%= recipe.title %></h2>
      <% end %>
    </li>
  <% end %>
</ul>
