<li>
  <div class="mark-as-ok"><i class="fa fa-check"></i></div>
  <div class="ingredient"><%= link_to ingredient.name, ingredient_path(ingredient) %></div>
  <div class="quantity">
    <div class="qt"><%= ingredient.description %></div>
    <% if ingredient.notes.present? %>
      <div class="qt-notes"><%= ingredient.notes %></div>
    <% end %>
  </div>
</li>
